{
  "name": "resume-jekyll",
  "version": "1.0.0",
  "description": "descr",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "jekyll build",
    "build:watch": "jekyll build --watch",
    "serve": "live-server --open=_site --wait=300",
    "stop": "pkill -f live-server && pkill -f live-server"
  },
  "keywords": [
    "keyword"
  ],
  "author": "Vadim",
  "license": "MIT",
  "dependencies": {
    "simple-slider": "^1.0.0",
    "wow.js": "^1.2.2"
  }
}
